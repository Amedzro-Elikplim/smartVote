<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartvote">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".reports_activity" android:label="report"></activity>
        <activity android:name=".votesCountActivity" android:label="details" />
        <activity
            android:name=".publishSuggestion"
            android:label="publish" />
        <activity
            android:name=".user_info_activity"
            android:label="update profile" />
        <activity
            android:name=".admin_activity"
            android:label="Admin home" />
        <activity
            android:name=".admin_validation"
            android:label="validation" />
        <activity
            android:name=".sign_up_activity"
            android:label="sign up" />
        <activity android:name=".login_in_activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="Feeds" />
    </application>

</manifest>